'use client';

// Hero is now integrated into RestaurantInfoCard — this component is kept as a no-op for import compatibility
export function RestaurantHero({ restaurant: _restaurant }: { restaurant: unknown }) {
  return null;
}
