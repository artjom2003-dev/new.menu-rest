# HTTP → HTTPS redirect
server {
    listen 80;
    server_name new.menu-rest.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name new.menu-rest.com;

    ssl_certificate     /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # Dynamic DNS resolver (Docker internal DNS)
    resolver 127.0.0.11 valid=10s ipv6=off;
    set $frontend_upstream frontend:3000;
    set $backend_upstream backend:3001;
    set $garage_upstream garage:3900;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Gzip
    gzip on;
    gzip_vary on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml image/svg+xml;
    gzip_min_length 1000;

    # Next.js static files
    location /_next/static/ {
        proxy_pass http://$frontend_upstream;
        proxy_cache_valid 200 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # API → NestJS backend
    location /api/ {
        proxy_pass http://$backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 60s;

        # Rate limiting
        limit_req zone=api burst=20 nodelay;
    }

    # Swagger docs
    location /api/docs {
        proxy_pass http://$backend_upstream;
        proxy_set_header Host $host;
    }

    # Garage S3 objects (фотографии ресторанов)
    location /photos/ {
        proxy_pass http://$garage_upstream/menurest-photos/;
        proxy_set_header Host menurest-photos.s3.garage.localhost;
        proxy_cache_valid 200 7d;
        add_header Cache-Control "public, max-age=604800";
    }

    # Frontend → Next.js (все остальные маршруты)
    location / {
        proxy_pass http://$frontend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # robots.txt
    location = /robots.txt {
        proxy_pass http://$frontend_upstream;
        add_header Cache-Control "public, max-age=86400";
    }

    # sitemap
    location ~ ^/sitemap {
        proxy_pass http://$frontend_upstream;
        add_header Cache-Control "public, max-age=3600";
    }
}

# Rate limiting zones
limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
