// Location management is no longer separate — address fields live on Restaurant directly.
// This file is kept as an empty stub to avoid broken imports during transition.
import { Injectable } from '@nestjs/common';

@Injectable()
export class LocationService {}
